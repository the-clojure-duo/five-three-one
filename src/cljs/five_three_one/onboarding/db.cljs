(ns five-three-one.onboarding.db)

(def initial-state
  {:onboarding
   {:real-max {:bench-press nil
               :deadlift nil
               :squat nil
               :press nil}
    :section :find-1rm}})
